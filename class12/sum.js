function totalThrough(n) {
    var numbers = new Array(n);
    for (i=0; i < n; i++) {
      numbers[i] = i;
    }
    total = 0;
    for (number of numbers) {
      total += number;
    }
    return total;
}

console.log("The total is " + totalThrough(100))
